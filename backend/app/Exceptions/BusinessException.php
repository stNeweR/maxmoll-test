<?php

namespace App\Exceptions;

use RuntimeException;

/**
 * Исключение бизнес-логики (предметной области).
 *
 * Выбрасывается, когда операция не может быть выполнена по правилам
 * приложения (например, недостаточно товара, нельзя изменить выполненный
 * заказ). Глобальный обработчик превращает его в ответ с кодом 422.
 */
class BusinessException extends RuntimeException
{
}